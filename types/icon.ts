import type { LucideIcon } from 'lucide-react';

export interface IconProps {
  icon: LucideIcon;
  size?: number;
  strokeWidth?: number;
  color?: string;
  className?: string;
  ariaLabel?: string;
}
